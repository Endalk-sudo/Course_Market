# **Course Market**
Course marker is a react app used to sell courses online it has a responsive and modern design. 

To use this project first clone it using: 

```
git clone "https://github.com/Endalk-sudo/Course_Market.git"
```

Then install using npm:

```
npm install
```

Then you can run it using:

```
npm run dev
```